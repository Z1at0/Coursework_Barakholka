{
  "name": "barakholka",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Барахолка — SPA для продажи подержанных вещей. Курсовая работа.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  }
}
